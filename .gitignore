# ---------------------------------------------------------------------------
# Generated build trees
# ---------------------------------------------------------------------------
# deps/repos/                shared source clones (one per dep)
# deps/builds/<plat>/<cfg>/  per-config dep scratch + install trees
# builds/<plat>/<cfg>/       Sneeze's own CMake scratch, lib/, bin/

deps/repos/
deps/builds/
builds/

# IDE and editor files
.vs/
.vscode/
*.user
*.suo
*.ncb
*.sdf
*.opensdf

# OS files
Thumbs.db
.DS_Store
